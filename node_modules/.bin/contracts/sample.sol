pragma solidity >= 0.5.0 < 0.7.0;
contract smartadvocate{
 

struct files_uploads{

	// State variables are fr_id,crime_no....etc sol filename is fingerprint 
	uint file_id;
	uint case_id;
	string file_title;
	string file_path;

C:\Users\sande\OneDrive\Desktop\Smart Advocate(main project)\node_modules\.bin\build\contracts

}


files_uploads []fu;


function add_files_uploads(uint file_id,
	uint case_id,
	
	string memory file_title,
	string memory file_path)
	public
	{
	files_uploads memory f1=files_uploads(file_id,case_id,file_title,file_path);
	fu.push(f1);
	}





}
